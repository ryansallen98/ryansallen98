---
import Layout from "@layouts/BaseLayout.astro";
import quote from "@data/general/quote.yml";
import QuoteWizard from "./_components/QuoteWizard";
import NetlifyQuoteForm from "./_components/NetlifyQuoteForm.astro";

const quoteConfig = {
  hero: quote.hero,
  wizard: quote.wizard,
  estimates: quote.estimates,
  result: quote.result,
};
---

<Layout title={quote.hero.title} seo={quote.seo} chrome={false}>
  <NetlifyQuoteForm />
  <QuoteWizard config={quoteConfig} client:load />
</Layout>
